apiVersion: v1
kind: Secret

metadata:
  name: {{ .Release.Name }}-postgres-secret

type: Opaque

stringData:
  POSTGRES_PASSWORD: {{ required "Set postgres.password" .Values.postgres.password | quote }}